{{!--thankyou--}}

<h1>{{thanks}}</h1>
<br>
<img src="{{dataUrl}}">

<form action="/thankyou" method="POST">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <button id="deleteSig">🗑</button>
</form>

<a href="/signers">
    <h3>
        {{signatures}}
    </h3>
</a>