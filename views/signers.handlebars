{{!--signers--}}

<h2>Here are all the lovely people who've signed this petition</h2>

<table>
    {{#names}}
    <tr>
        <td>
            {{#if homepage}}<a href="{{homepage}}"> {{first}} {{last}}</a>{{else}} {{first}}
            {{last}}{{/if}}{{#if age}}, {{age}}
            {{/if}} {{#if city}}<a href="/signers/{{city}}"> | {{city}}</a> {{/if}}
        </td>
    </tr>
    {{/names}}

</table>